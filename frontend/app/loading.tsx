export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return <h1>This is the loading screen. Please do not refresh the page while the image loads to avoid using up limited API credits!</h1>
  }